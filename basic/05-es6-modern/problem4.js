/**
 * 05-es6-modern / 問題4: 分割代入（配列）
 * 
 * 【課題】
 * 以下の配列から、分割代入を使って最初の2つの要素を取得してください
 * 
 * const colors = ["red", "green", "blue", "yellow"];
 * 
 * 分割代入を使って、firstColorとsecondColorに値を代入してください
 * 
 * 【要求事項】
 * - 分割代入構文を使用する
 * - 変数名は firstColor, secondColor とする
 * 
 * 【目安時間】5分
 */

// 与えられた配列（変更不要）
const colors = ["red", "green", "blue", "yellow"];

// ========== ここから回答を記述 ==========

// ここに回答を記述してください


// ========== ここまで回答を記述 ==========

// 動作確認用のコード（変更不要）
console.log('=== 問題4の実行結果 ===');
try {
    console.log('firstColor:', firstColor); // 期待値: "red"
    console.log('secondColor:', secondColor); // 期待値: "green"
    console.log('元の配列:', colors);
    console.log('✅ 正常に実行されました');
} catch (error) {
    console.log('❌ エラー:', error.message);
}
