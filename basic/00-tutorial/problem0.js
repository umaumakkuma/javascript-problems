/**
 * 00-tutorial / 問題0: この問題集の使い方（チュートリアル）
 *
 * ようこそ！これは使い方に慣れるための練習問題で、採点には含まれません。
 *
 * この問題集のすべての問題は、次の3つの部分でできています：
 *   1. 問題文コメント …… いま読んでいるこの部分
 *   2. 回答欄 …………… 「ここから回答を記述」と「ここまで」のマーカーの間。
 *                          回答は必ずこの中に書きます
 *   3. 動作確認用のコード … 実行すると回答を検証して ✅ / ❌ を表示します。
 *                          この部分は変更しません
 *
 * 【課題】
 * 変数 message に文字列 "こんにちは、JavaScript!" を代入してください。
 * ※ この問題0だけは、最初から回答が書き込んであります。
 *
 * 【この問題でやってみること】
 * 1. まずこのまま実行して、✅ が表示されるのを確認する（これが「正解」の状態）：
 *      node basic/00-tutorial/problem0.js
 * 2. 回答欄の1行を削除して、もう一度実行する
 *      → 「❌ エラー: message is not defined」と表示されます（これが「未回答」の状態）。
 *        エラーメッセージの読み方は docs/how-to-read-errors.md を参照してください
 * 3. 削除した行を見ないで自分で書き直して、もう一度 ✅ に戻す
 *
 * 【ほかに知っておくと便利なこと】
 * - 回答状況の一括チェック: cd basic && node check.js（この問題0は集計対象外です）
 * - 解けない問題は、問題文の【参考】にあるJavaScript Primerの章を読んでから再挑戦
 *
 * 【目安時間】5分
 */

// ========== ここから回答を記述 ==========

const message = "こんにちは、JavaScript!";

// ========== ここまで回答を記述 ==========

// 動作確認用のコード（変更不要）
console.log('=== 問題0の実行結果 ===');
try {
    console.log('message:', message); // 期待値: "こんにちは、JavaScript!"
    console.log('✅ 正常に実行されました');
} catch (error) {
    console.log('❌ エラー:', error.message);
}
