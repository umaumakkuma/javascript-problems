/**
 * 10-arrays / 問題1: 要素の追加と削除
 *
 * 【課題】
 * 配列の先頭・末尾への要素の追加と削除を行ってください：
 * - "りんご" だけを持つ配列 fruits を作成
 * - push()で末尾に "バナナ" と "みかん" を追加
 * - shift()で先頭の要素を取り除き、その値を変数 removedFruit に代入
 * - unshift()で先頭に "いちご" を追加
 *
 * 【要求事項】
 * - push / shift / unshift を使用する
 * - それぞれのメソッドが「配列の先頭・末尾のどちらに作用するのか」を
 *   コメントで説明する
 *
 * 【参考】JavaScript Primer「配列」
 * https://jsprimer.net/basic/array/
 *
 * 【目安時間】5分
 */

// ========== ここから回答を記述 ==========

// ここに回答を記述してください


// ========== ここまで回答を記述 ==========

// 動作確認用のコード（変更不要）
console.log('=== 問題1の実行結果 ===');
try {
    console.log('fruits:', fruits);             // 期待値: ["いちご", "バナナ", "みかん"]
    console.log('removedFruit:', removedFruit); // 期待値: "りんご"
    console.log('✅ 正常に実行されました');
} catch (error) {
    console.log('❌ エラー:', error.message);
}
