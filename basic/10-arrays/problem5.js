/**
 * 10-arrays / 問題5: 破壊的なメソッドと非破壊的な操作
 *
 * 【課題】
 * sort()は元の配列を変更してしまう「破壊的メソッド」です。
 * 元の配列を変更せずにソートする処理を書いてください：
 *
 * const original = [3, 1, 10, 2];
 *
 * - originalを変更せずに昇順ソートした新しい配列を作成し、
 *   変数 sorted に代入（スプレッド構文でコピーしてからsort）
 * - sort()に比較関数を渡さないと[1, 10, 2, 3]のように文字列順で
 *   並んでしまうため、数値用の比較関数 (a, b) => a - b を渡す
 *
 * 【要求事項】
 * - originalは自分で定義する
 * - ソート後もoriginalが[3, 1, 10, 2]のままであることを確認する
 * - 「破壊的メソッド（元の配列を変更する）」と「非破壊的メソッド（新しい
 *   配列を返す）」の違いをコメントで説明する
 *
 * 【参考】JavaScript Primer「配列」
 * https://jsprimer.net/basic/array/
 *
 * 【目安時間】15分
 */

// ========== ここから回答を記述 ==========

// ここに回答を記述してください


// ========== ここまで回答を記述 ==========

// 動作確認用のコード（変更不要）
console.log('=== 問題5の実行結果 ===');
try {
    console.log('sorted:', sorted);     // 期待値: [1, 2, 3, 10]
    console.log('original:', original); // 期待値: [3, 1, 10, 2]（変更されていない）
    console.log('✅ 正常に実行されました');
} catch (error) {
    console.log('❌ エラー:', error.message);
}
