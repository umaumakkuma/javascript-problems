/**
 * 02-functions / 問題5: 関数の呼び出し
 * 
 * 【課題】
 * 与えられた関数を使って、指定された処理を実行してください
 * 1. greet関数を使って"山田"さんへの挨拶メッセージを作成し、変数messageに代入
 * 2. そのメッセージをconsole.logで出力
 * 
 * 【目安時間】5分
 */

// 与えられた関数（変更不要）
function greet(name) {
    return "こんにちは、" + name + "さん！";
}

// ========== ここから回答を記述 ==========

// ここに回答を記述してください


// ========== ここまで回答を記述 ==========

// 動作確認用のコード（変更不要）
console.log('=== 問題5の実行結果 ===');
try {
    console.log('作成されたメッセージ:', message);
    console.log('✅ 正常に実行されました');
} catch (error) {
    console.log('❌ エラー:', error.message);
}
