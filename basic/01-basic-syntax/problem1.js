/**
 * 01-basic-syntax / 問題1: 変数宣言と値の代入
 * 
 * 【課題】
 * 以下の変数を適切に宣言してください：
 * - 年齢を格納する変数（値は変更される可能性がある）
 * - 名前を格納する変数（値は変更されない）
 * - 生年月日を格納する変数（値は変更されない）
 * 
 * 【要求事項】
 * - let と const を適切に使い分ける
 * - 変数名は分かりやすい名前を付ける
 * - 初期値を設定する
 * 
 * 【目安時間】5分
 */

// ========== ここから回答を記述 ==========

// ここに回答を記述してください


// ========== ここまで回答を記述 ==========

// 動作確認用のコード（変更不要）
console.log('=== 問題1の実行結果 ===');
try {
    console.log('年齢:', age);
    console.log('名前:', name);
    console.log('生年月日:', birthDate);
    console.log('✅ 正常に実行されました');
} catch (error) {
    console.log('❌ エラー:', error.message);
}
